
# source_texture.py

def write_fix(file, game):

                

def main():
    game = 'portal2'
    starting_path = 'C:\\Program Files (x86)\\Steam\\steamapps\\common\\Counter-Strike Global Offensive\\csgo\\materials\\portal2\\br_debris'
    for root, dirs, files in os.walk(starting_path):
        for file in files: if file.endswith('.vmt'):
            with open(file) as text:
                with open()
                    for line in text: 
                        if '/' in line and game not in line:

if __name__ == '__main__':
    import os, sys
    main()
